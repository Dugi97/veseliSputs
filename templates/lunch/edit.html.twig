{% extends 'base.html.twig' %}

{% block title %}Edit Lunch{% endblock %}

{% block body %}
    <h1>Edit Lunch</h1>

    {{ include('lunch/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('lunch_index') }}">back to list</a>

    {{ include('lunch/_delete_form.html.twig') }}
{% endblock %}
